# Generated by `tidal-server init-config`.
# Copy to `~/.tidal/server/.env`, or point `TIDAL_ENV_FILE` at the file you actually use.
# Secrets only. This file is for server-side runtime secrets and optional broadcast creds.
# These defaults intentionally stay in code unless you need an override:
# TIDAL_API_HOST, TIDAL_API_PORT, TOKEN_PRICE_AGG_BASE_URL,
# AUCTIONSCAN_BASE_URL, AUCTIONSCAN_API_BASE_URL
RPC_URL=
TOKEN_PRICE_AGG_KEY=
TXN_KEYSTORE_PATH=
TXN_KEYSTORE_PASSPHRASE=
